apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: jira
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:13
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: jira
            - name: POSTGRES_USER
              value: jira
            - name: POSTGRES_PASSWORD
              value: jira
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: pgdata
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: jira
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
